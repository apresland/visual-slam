#include "optimization.h"

void Optimization::compute(std::unordered_map<unsigned long, std::shared_ptr<Frame>> keyframes,
                           std::unordered_map<unsigned long, MapPoint> landmarks,
                           const cv::Mat &K) {

}
